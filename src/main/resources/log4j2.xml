<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Properties>
        <Property name="log-path">allure-results/Logs</Property>
        <Property name="timestamp">${date:yyyy-MM-dd-HH-mm-ss}</Property>
    </Properties>
    <Appenders>

        <RollingFile
                name="RollingFile"
                fileName="${log-path}/Run-${timestamp}.log"
                filePattern="${log-path}/archive/run-%d{yyyy-MM-dd}-%i.log.gz">

            <PatternLayout>
                <Pattern>%d{HH:mm:ss} %-5p [%c{1}] %m%n</Pattern>
            </PatternLayout>

            <Policies>
                <SizeBasedTriggeringPolicy size="5MB"/>
            </Policies>

            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <Console name="Console">
            <PatternLayout pattern="%d{HH:mm:ss} %-5p [%c{1}] %m%n"/>
        </Console>

    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Root>
    </Loggers>

</Configuration>